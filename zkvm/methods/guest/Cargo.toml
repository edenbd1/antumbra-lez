[package]
name    = "antumbra-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
risc0-zkvm     = { version = "3.0.5", default-features = false, features = ["std"] }
antumbra-curve = { path = "../../.." }

[profile.release]
overflow-checks = true

[workspace]
